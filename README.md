# conditional-gan-celeba
Modello generativo addestrato su CelebA per la sintesi di volti fotorealistici. Usa un Projection Discriminator per controllare attributi visivi (genere, età, sorriso) e integra tecniche avanzate (TTUR, Instance Noise, class balancing) per stabilizzare il training e prevenire il mode collapse.
